package TDPCalculator.common.enums;

public enum MessageType {  // declaring an enum using the enum keyword
    // defining enum constants using the enum keyword
    Information,
    Debug,
    Error
}